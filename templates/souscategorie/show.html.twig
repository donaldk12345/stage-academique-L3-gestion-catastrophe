{% extends 'base.html.twig' %}

{% block title %}Souscategorie{% endblock %}

{% block body %}
    <h1>Souscategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ souscategorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ souscategorie.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_souscategorie_index') }}">back to list</a>

    <a href="{{ path('app_souscategorie_edit', {'id': souscategorie.id}) }}">edit</a>

    {{ include('souscategorie/_delete_form.html.twig') }}
{% endblock %}
