{% extends 'base.html.twig' %}

{% block title %}Gestion des Catastrophes!{% endblock %}

{% block body %}
<style>
 
</style>

<div class="main-wrapper">
   {% include "partials/_header.html.twig" %}
   
   {% include "partials/_sidebar.html.twig" %}

<div class="page-wrapper mt-4">
<div class="row">
<div class="col-md-11 d-flex m-auto">

<div class="card card-table flex-fill">
<div class="card-header">
<h4 class="card-title  text-center">Listes des Utilisateurs</h4>
</div>
<div class="card-body">
<div class="table-responsive no-radius">
<table class="table table-hover table-center">
<thead>
<tr>
<th>Photos</th>
<th>Prenom</th>
<th class="text-center">Date de création du compte</th>
<th class="text-center">email</th>
<th class="text-end">Actions</th>
</tr>
</thead>
<tbody>
 {% for user in users %}
<tr>
<td class="text-nowrap">
<div class="font-weight-600">
  <a href="#" class="avatar avatar-sm me-2"><img class="avatar-img rounded-circle" src="/images/blog/{{  user.imageUser  }}" alt="User Image"></a>
</h2>
</div>
</td>
<td class="text-nowrap"> {{ user.username }} </td>
<td class="text-center">{{ user.createdAt | date ('d/m/y') }} à {{ user.createdAt | date ('h:i') }}</td>
<td class="text-center">  {{  user.email }} </td>
<td class="text-end">
<div class="actions">
<a href="#" class="btn btn-sm bg-success-light me-2">
<i class="fas fa-pencil"></i>
</a>
<a href="#" class="btn btn-sm bg-danger-light">
<i class="fas fa-trash"></i>
</a>
</div>
</td>
</tr>
 {% endfor %}
</tbody>
</table>
</div>
</div>
</div>

</div>
</div>
</div>
</div>

</div>


{% endblock %}
